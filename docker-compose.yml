services:
  postgres:
    image: postgres:16-alpine
    container_name: fintech-postgres
    environment:
      POSTGRES_DB: fintech
      POSTGRES_USER: fintech
      POSTGRES_PASSWORD: fintech_secret
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fintech -d fintech"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: fintech-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: fintech-platform/Dockerfile
    container_name: fintech-backend
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: fintech
      DB_USER: fintech
      DB_PASSWORD: fintech_secret
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: dev-jwt-secret-key-with-minimum-256-bits-length-required
      AES_KEY: dev-aes-key-32-characters-long!!
      SPRING_PROFILES_ACTIVE: dev
    ports:
      - "8080:8080"

  notification-service:
    build:
      context: ./services/notification-service
      dockerfile: Dockerfile
    container_name: fintech-notifications
    ports:
      - "8090:8090"

volumes:
  postgres_data:
