apiVersion: apps/v1
kind: Deployment
metadata:
  name: fintech-backend
  namespace: fintech
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fintech-backend
  template:
    metadata:
      labels:
        app: fintech-backend
    spec:
      containers:
        - name: backend
          image: fintech/backend:latest
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: fintech-backend-secrets
          readinessProbe:
            httpGet:
              path: /api/actuator/health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/actuator/health
              port: 8080
            initialDelaySeconds: 40
            periodSeconds: 20
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: fintech-backend
  namespace: fintech
spec:
  selector:
    app: fintech-backend
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
