apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-admin
  namespace: met
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-admin
  template:
    metadata:
      labels:
        app: web-admin
    spec:
      containers:
        - name: web-admin
          image: met/web-admin:1.0.0
          ports:
            - containerPort: 3001
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: web-admin
  namespace: met
spec:
  selector:
    app: web-admin
  ports:
    - port: 80
      targetPort: 3001
  type: ClusterIP
